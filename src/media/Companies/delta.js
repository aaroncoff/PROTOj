import React, { Component } from 'react';
import delta from './delta.png';

export default class Delta extends Component {
    render() {
        return(
            <div><img className="Delta" src={delta}  /></div>
        )
    }
}